import torch
import torch.optim as optim
import torch.nn as nn


class SegNet(nn.Module):

    def __init__(self):
        super(SegNet, self).__init__()
        # WHEN DOWNLOADING DATASET SEPARATE INTO TRAIN AND VALIDATION SETS

    def forward(self, x):
        return x
